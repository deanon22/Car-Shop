<div class="max-w-3xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">Edit Maintenance Job</h1>
    <p class="text-gray-500 text-sm mt-1">Updating record for <%= @car.year %> <%= @car.make %> <%= @car.model %>.</p>
  </div>

  <div class="bg-white rounded-2xl shadow-sm border border-gray-100 p-6 md:p-8">
    <%= render 'form', car: @car, maintenance_job: @maintenance_job %>
  </div>
</div>
